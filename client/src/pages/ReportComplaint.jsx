import { useEffect, useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { toast } from 'react-toastify'
import { useAppDispatch, useAppSelector } from '../app/hooks.js'
import { checkDuplicate, clearDuplicates, createComplaint } from '../features/complaintsSlice.js'
const categories=['Road','Garbage','Water','Electricity','Other']
export default function ReportComplaint(){const [form,setForm]=useState({title:'',category:'Road',description:'',area:''});const [checking,setChecking]=useState(false);const dispatch=useAppDispatch();const nav=useNavigate();const {duplicates}=useAppSelector(s=>s.complaints);useEffect(()=>{if(form.category&&form.area.trim()){const t=setTimeout(async()=>{setChecking(true);await dispatch(checkDuplicate({category:form.category,area:form.area}));setChecking(false)},350);return()=>clearTimeout(t)}dispatch(clearDuplicates())},[dispatch,form.category,form.area]);const submit=async e=>{e.preventDefault();const r=await dispatch(createComplaint(form));if(createComplaint.fulfilled.match(r)){toast.success('Complaint submitted');nav('/dashboard')}else if(r.payload?.duplicates?.length){toast.warning('A similar complaint already exists. Consider upvoting it instead.')}else toast.error(r.payload?.message||'Unable to submit')};return <div className="mx-auto max-w-2xl"><div className="mb-7"><p className="font-bold text-emerald-700">New complaint</p><h1 className="text-3xl font-black text-slate-900">Report a civic problem</h1><p className="mt-1 text-slate-500">Tell us what happened and where. Keep it clear and specific.</p></div>{duplicates.length>0&&<div className="mb-5 rounded-2xl border border-amber-200 bg-amber-50 p-5"><b className="text-amber-900">A similar complaint may already exist</b><p className="mt-1 text-sm text-amber-800">{checking?'Checking...':'We found active complaints in this category and area. You can browse and upvote one instead.'}</p><div className="mt-3 space-y-2">{duplicates.map(d=><Link key={d._id} to={`/complaints/${d._id}`} className="block rounded-xl bg-white p-3 text-sm font-semibold text-emerald-700">{d.title} · {d.upvotes} upvotes</Link>)}</div></div>}<form onSubmit={submit} className="space-y-5 rounded-2xl border border-slate-200 bg-white p-7 shadow-sm"><label className="block text-sm font-semibold">Complaint title<input required maxLength={120} placeholder="e.g. Large pothole near main market" value={form.title} onChange={e=>setForm({...form,title:e.target.value})} className="mt-1.5 w-full rounded-xl border border-slate-300 px-4 py-3"/></label><div className="grid gap-5 md:grid-cols-2"><label className="block text-sm font-semibold">Category<select value={form.category} onChange={e=>setForm({...form,category:e.target.value})} className="mt-1.5 w-full rounded-xl border border-slate-300 px-4 py-3">{categories.map(c=><option key={c}>{c}</option>)}</select></label><label className="block text-sm font-semibold">Area / locality<input required placeholder="e.g. Gulshan" value={form.area} onChange={e=>setForm({...form,area:e.target.value})} className="mt-1.5 w-full rounded-xl border border-slate-300 px-4 py-3"/></label></div><label className="block text-sm font-semibold">Description<textarea required rows="6" maxLength={2000} placeholder="Describe the issue and any useful details..." value={form.description} onChange={e=>setForm({...form,description:e.target.value})} className="mt-1.5 w-full resize-none rounded-xl border border-slate-300 px-4 py-3"/></label><button className="w-full rounded-xl bg-emerald-600 py-3.5 font-bold text-white hover:bg-emerald-700">Submit complaint</button></form></div>}
